# PRD: v0.3.0 — Kova Status Line Indicator

> Generated by Kova Init — 2026-02-22
> Run with: `/kova:loop docs/prd-v030-statusline.md`

## Overview

Add kova activation status to the Claude Code status line so users can see at a glance whether kova hooks are active, inactive, or running a loop — directly in their Claude Code session.

## Items

- [x] Create a `kova-statusline.sh` script that detects kova activation state (active/inactive/loop running) and outputs a colored indicator, designed to be sourced or called from any user's existing statusline script
- [x] Update `install.sh` to install the statusline integration — inject kova indicator into the user's existing statusline script or create one if none exists, with backup of the original
- [x] Add `kova activate`/`kova deactivate` awareness — ensure the statusline reflects real-time hook state by checking `.claude/settings.json` for registered kova hooks
- [x] Add tests for the statusline script (unit tests for each state: active, inactive, loop-running) and integration tests for the install flow
- [x] Update README.md and docs with the new statusline feature, including screenshots/examples of what it looks like

## Notes

- Tech stack: Bash (shell scripts), jq for JSON parsing
- Dependencies: None new — uses existing Claude Code statusline API (reads JSON from stdin, outputs formatted text to stdout)
- The statusline script receives session JSON via stdin from Claude Code — kova indicator logic runs independently by checking local file state
- Must not break existing user statusline scripts — additive only
- Assumptions: Users may or may not have an existing statusline configured; install must handle both cases
